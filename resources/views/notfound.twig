{% extends 'base.twig' %}

{% block content %}
<div class="row">
  <div class="alert alert-warning col-12 col-md-8 offset-md-2 text-center">
    <h1>
      <i class="fas fa-3x fa-exclamation-triangle"></i><br>
      Page not found    
    </h1>
    <hr>
    We couldn't find that page.
    {% if not hide_homepage_link %}
      <br>
      <a href="{{ link('') }}">Take me back to the homepage →</a>
    {% endif %}
  </div>
</div>
{% endblock %}
